# Layer Values Dashboard Environment Configuration
# Copy this file to .env and customize for your setup

# ===========================================
# INSTANCE CONFIGURATION
# ===========================================

# Instance name - used for data isolation and URL paths
# This creates instance-specific directories and mount paths
# Examples: palmito, mainnet, testnet
LAYER_INSTANCE_NAME=palmito

# Source directory for CSV data files
# If not specified, defaults to source_tables_{LAYER_INSTANCE_NAME}
# Each instance should have its own directory to prevent data sharing
LAYER_SOURCE_DIR=source_tables_palmito

# ===========================================
# NETWORK CONFIGURATION
# ===========================================

# RPC URL for connecting to the Layer Protocol node
# Used by the reporter fetcher to collect network data
# HTTP or WebSocket URLs supported
LAYER_RPC_URL=http://localhost:26657

# Alternative WebSocket URL example:
# LAYER_RPC_URL=ws://localhost:26657

# For remote nodes:
# LAYER_RPC_URL=http://your-layer-node.example.com:26657

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# Server host (usually 0.0.0.0 for external access)
HOST=0.0.0.0

# Server port (use different ports for multiple instances)
PORT=8001

# ===========================================
# MULTIPLE INSTANCE EXAMPLES
# ===========================================

# Example configuration for Palmito Testnet instance:
# LAYER_INSTANCE_NAME=palmito
# LAYER_SOURCE_DIR=source_tables_palmito
# LAYER_RPC_URL=http://localhost:26657
# PORT=8001

# Example configuration for Mainnet instance:
# LAYER_INSTANCE_NAME=mainnet
# LAYER_SOURCE_DIR=source_tables_mainnet
# LAYER_RPC_URL=http://localhost:26658
# PORT=8002

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# Database memory limit (optional, auto-detected based on system RAM)
# DB_MEMORY_LIMIT=8GB

# Number of database threads (optional, defaults to 3)
# DB_THREADS=3

# Temporary directory for database operations
# DB_TEMP_DIR=/tmp/duckdb

# ===========================================
# DEVELOPMENT CONFIGURATION
# ===========================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable debug mode (true/false)
DEBUG=false

# Reload server on code changes (true/false)
RELOAD=false

# ===========================================
# USAGE INSTRUCTIONS
# ===========================================

# 1. Copy this file to .env:
#    cp env.example .env

# 2. Edit .env with your specific configuration

# 3. Run the dashboard:
#    python start_dashboard.py

# 4. Or set environment variables directly:
#    export LAYER_INSTANCE_NAME=palmito
#    export LAYER_SOURCE_DIR=source_tables_palmito
#    python start_dashboard.py

# 5. For multiple instances, create separate .env files:
#    .env.palmito, .env.mainnet, etc.
#    Then source the appropriate one before starting:
#    source .env.palmito && python start_dashboard.py

# ===========================================
# TROUBLESHOOTING
# ===========================================

# If instances show the same data:
# - Ensure LAYER_INSTANCE_NAME is different for each instance
# - Ensure LAYER_SOURCE_DIR is different for each instance
# - Use different PORT numbers for each instance

# If dashboard is not accessible:
# - Check the correct URL: http://localhost:{PORT}/dashboard-{INSTANCE_NAME}/
# - Ensure firewall allows the PORT

# If no data appears:
# - Check that CSV files exist in LAYER_SOURCE_DIR
# - Verify CSV files have the expected format
# - Check the dashboard logs for errors 